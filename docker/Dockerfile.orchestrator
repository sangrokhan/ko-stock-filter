FROM stock-trading-base:latest

# Switch to root to copy service code
USER root

# Copy service code
COPY services/orchestrator/ /app/services/orchestrator/

# Install service-specific dependencies if they exist
RUN if [ -f /app/services/orchestrator/requirements.txt ]; then \
    pip install --no-cache-dir -r /app/services/orchestrator/requirements.txt; \
    fi

# Set ownership and switch back to appuser
RUN chown -R appuser:appuser /app
USER appuser

WORKDIR /app/services/orchestrator

CMD ["python", "main.py"]
